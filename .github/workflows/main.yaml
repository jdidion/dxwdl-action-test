name: Master Workflow
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    name: Build Workflow
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - name: Compile hello.wdl
        uses: jdidion/dxwdl-action-test@master
        with:
          wdl: hello.wdl
          destination: project-FpGG6G80qpzP2PF4K2B2kzY8:/
          api_token: ${{ secrets.API_TOKEN }}

